classDiagram
    %% Clases del Modelo de Dominio (Reflejando el DER y Responsabilidades)

    class Cancha {
        +int id_cancha
        +String nombre
        +String hora_apertura
        +String hora_cierre
        +float precio
        +int id_deporte
        +validarDatos(nombre, tipo, apertura, cierre, precio)$ Object
        +obtenerHorarios(id_cancha, horaApertura, horaCierre)$ Array
        +calcularDisponibilidad(horarios, ocupados)$ Array
        +calcularDisponibilidadUI(cancha, fecha)$ Array
        +agregarCancha(nombre, id_deporte, nombre_deporte, apertura, cierre, precio)$ Object
        +getCanchas()$ Array
        +eliminarCancha(id_cancha)$ Object
        +actualizarCancha(id_cancha, datosFormulario)$ Object
    }
    
    class CanchaFutbol {}
    class CanchaPadel {}
    class CanchaTenis {}

    Cancha <|-- CanchaFutbol
    Cancha <|-- CanchaPadel
    Cancha <|-- CanchaTenis

    class Tipo_Deporte {
        +int id_deporte
        +String descripcion
        +obtenerTiposCancha()$ Array
    }

    class CanchaFactory {
        +crearCancha(id_cancha, nombre, id_deporte, nombre_deporte, hora_apertura, hora_cierre, precio)$ Cancha
    }

    Cancha ..> CanchaFactory : Utiliza para crear instancias

    class Jugador {
        +int id_jugador
        +String nombre
        +String email
        +String telefono
        +obtenerTodosJugadores()$ Array
        +buscarJugador(email)$ Object
        +crearJugador(email, nombre, telefono)$ Object
    }

    class Estado_Reserva {
        +int id_estado
        +String descripcion
        +obtenerEstado(descripcion)$ int
    }

    class Metodo_Pago {
        +int id_metodo
        +String descripcion
    }

    class Estado_Pago {
        +int id_pago
        +String estado
        +Date fecha_pago
        +int id_metodo
        +generarEstadoPago(estado, metodoPago)$ int
    }

    class Detalle_Reserva {
        +int id_detalle
        +Date fecha_reserva
        +String hora_reserva
        +float monto_total
        +int id_reserva
        +int id_pago
        +int id_cancha
        +buscarOcupacion(id_cancha, fecha)$ Array
        +validarSolapamiento(id_cancha, fecha, hora)$ boolean
        +generarDetalle(id_reserva, id_pago, id_cancha, fecha, hora, monto_total)$ void
        +liberarBloque(id_reserva)$ void
    }

    class Reserva {
        +int id_reserva
        +float total
        +int id_jugador
        +int id_estado
        +crearReserva(id_jugador, total, id_estado)$ Object
        +verificarEstado(id_reserva)$ boolean
        +modificarEstadoReserva(id_reserva, id_estado)$ void
        +confirmarReservaCompleta(email, nombre, telefono, turno, metodoPago)$ Object
        +obtenerReservasActivasPorEmail(email)$ Array
        +cancelarReserva(id_reserva)$ Object
    }

    %% Relaciones funcionales y estructurales
    Reserva ..> Jugador : Busca/Crea
    Reserva ..> Detalle_Reserva : Valida y Asocia
    Reserva ..> Estado_Pago : Genera
    Reserva ..> Estado_Reserva : Consulta y Modifica
    Cancha ..> Detalle_Reserva : Consulta ocupación
    Cancha ..> Tipo_Deporte : Solicita tipos de deporte
    Estado_Pago ..> Metodo_Pago : Utiliza

    class ConfigController {
        +getConfig()$ Object
        +updateConfig(datos)$ Object
    }

    class AuthService {
        +login(email, password)$ Object
        +logout()$ Object
        +requireAuth(redirectTo)$ Object
        +isAuthenticated()$ boolean
    }
